# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Data validation
pydantic==2.10.4
pydantic-settings==2.7.0

# Database
asyncpg==0.30.0
alembic==1.14.0
psycopg2-binary>=2.9.0


# Cache
# aioredis >= 2.x repackaged under redis.asyncio; use redis-py for both sync and async.
# persistence-schema.md imports aioredis — add the shim so that import works unchanged.
aioredis==2.0.1        # thin shim over redis.asyncio; kept for import compatibility
redis==5.2.1

# HTTP client
httpx==0.28.1

# Observability
structlog==24.4.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-httpx==0.35.0

# Dev utilities (optional; install separately)
# ruff, mypy, pre-commit
